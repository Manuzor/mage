import mage;

class HelloDepsInit : Target
{
  this()
  {
    globalProperties.set!"name" = "dependencies sample";
    globalProperties.set!"language" = "cpp";
    globalProperties.set!"includePaths" = [ cwd() ];
  }
}
