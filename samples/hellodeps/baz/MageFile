module baz;
import mage;
import bar;

class Baz : Executable
{
  @Dependency Bar bar;

  override void configure()
  {
    properties.set!"name" = "baz";
    properties.set!"sourceFiles" = [
      Path("baz.cpp")
    ];
    properties.set!"linkTargets" = [ bar ];
  }
}
